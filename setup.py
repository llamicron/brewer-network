from distutils.core import setup
setup(
    name='brewer-network',
    packages=['brewer-network'],  # this must be the same as the name above
    version='0.1',
    description='The networking portion of brewer',
    author='Luke Sweeney',
    author_email='luke@thesweeneys.org',
    url='https://github.com/llamicron/brewer-network',  # use the URL to the github repo
    download_url=
    '',  # I'll explain this in a second
    keywords=['brewer', 'network'],  # arbitrary keywords
    classifiers=[],
)
